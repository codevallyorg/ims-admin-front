import Private from '@/components/layout/Private';
import { withLayout } from '@/components/layout/utils';
import { FC } from 'react';

const Users: FC = () => {
  return <></>;
};

export default withLayout(Users, Private);
